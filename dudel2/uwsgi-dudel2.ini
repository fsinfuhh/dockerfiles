[uwsgi]

procname-master = uwsgi %n
master = true
socket = :3008

plugins = python

chdir = /opt/dudel2

home = /opt/dudel2/.pyenv
module = dudel.wsgi:application
env = DJANGO_SETTINGS_MODULE=dudel.settings
env = LANG='en_US.UTF-8'

; drop privileges
uid    = www-data
gid    = www-data
umask  = 027

; run with at least 1 process but increase up to 4 when needed
processes = 8
cheaper = 2

; reload whenever this config file changes
; %p is the full path of the current config file
touch-reload = %p

; disable uWSGI request logging
disable-logging = true

enable-threads = true

static-map = /static=/opt/dudel2/_static
static-map = /media=/opt/dudel2/_media
static-safe = /
